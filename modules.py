# A module is a file containing a set of functions to include in an application.
# There are core python modules, modules you install with pip, and custom modules.

import datetime

today = datetime.date.today()

print(today)